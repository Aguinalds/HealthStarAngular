import { FormatoRealPipe } from './formato-real.pipe';

describe('FormatoRealPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoRealPipe();
    expect(pipe).toBeTruthy();
  });
});
